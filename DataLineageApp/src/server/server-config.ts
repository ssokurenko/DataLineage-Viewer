const config = {
    iotaProviders: ["https://nodes.iota.fm", "https://iotanode.us:443"],
    pacakgeCacheSeconds: 3600 * 24 * 3,
    proxy: {
        host: "127.0.0.1",
        port: 1080
    }
};

export default config;
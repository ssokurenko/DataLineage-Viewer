import * as ReactDOM from "react-dom";
import * as React from "react";
import { Publisher } from "./publisher";

ReactDOM.render(<Publisher inputsAddress={[]}/>, document.getElementById("simlate-publisher-app"));